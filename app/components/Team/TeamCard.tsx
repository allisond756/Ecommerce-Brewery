import React from 'react'

interface Props {
    image: string;
    name: string;
    position: string;
}

const TeamCard = ({}:Props) => {
    return (
        <div>TeamCard</div>
    )
}

export default TeamCard